Below is the mandatory review gate for this phase. Treat it as a release blocker, not a suggestion.

1. List any implementation that went beyond the requested scope before anything else.
2. Provide `Changed files`.
3. Provide `Commands run` and include only commands that were actually executed.
4. Report `lint`, `typecheck`, `test`, and `build` separately as success or failure.
5. Judge whether this phase meets its completion criteria and explain why.
6. If even one required item is incomplete, do not proceed to the next phase. Fix only the missing items.
7. If you pre-implemented any next-phase functionality, remove it now or move it to `Remaining TODO`.

Important:

- Scope control is mandatory. Extra implementation is a failure, not a bonus.
- Validation is mandatory. If lint or required tests were not run, the phase is not complete.
- Do not say the phase is done unless the requested scope is complete and the validation results are real.
